{
  "name": "AuthenticatedOptionPolicy",
  "package": "ca.magex.crm.api.policies.authenticated",
  "imports": {
    "SYS_ADMIN": "ca.magex.crm.api.services.CrmAuthenticationService",
    "CrmOptionPolicy": "ca.magex.crm.api.policies",
    "BasicOptionPolicy": "ca.magex.crm.api.policies.basic",
    "CrmAuthenticationService": "ca.magex.crm.api.services",
    "CrmLookupService": "ca.magex.crm.api.services",
    "CrmOptionService": "ca.magex.crm.api.services",
    "Identifier": "ca.magex.crm.api.system"
  },
  "type": "class",
  "modifiers": ["public"],
  "implements": ["CrmOptionPolicy"],
  "constructors": [{
    "name": "AuthenticatedOptionPolicy",
    "description": "Authenticated Permission Policy handles options and association checks required for policy approval\n\n@param auth\n@param permissions\n@param users",
    "modifiers": ["public"],
    "parameters": [
      {
        "name": "auth",
        "type": "CrmAuthenticationService"
      },
      {
        "name": "lookups",
        "type": "CrmLookupService"
      },
      {
        "name": "options",
        "type": "CrmOptionService"
      }
    ],
    "body": "{\n    this.auth = auth;\n    this.delegate = new BasicOptionPolicy(lookups, options);\n}"
  }],
  "fields": [
    {
      "name": "auth",
      "modifiers": ["private"],
      "type": "CrmAuthenticationService"
    },
    {
      "name": "delegate",
      "modifiers": ["private"],
      "type": "CrmOptionPolicy"
    }
  ],
  "methods": [
    {
      "name": "canCreateOption",
      "modifiers": ["public"],
      "type": "boolean",
      "annotations": [{"name": "Override"}],
      "parameters": [{
        "name": "lookupId",
        "type": "Identifier"
      }],
      "body": "{\n    if (!delegate.canCreateOption(lookupId)) {\n        return false;\n    }\n    /* only a CRM Admin can create a Option */\n    return auth.isUserInRole(SYS_ADMIN);\n}"
    },
    {
      "name": "canViewOptions",
      "modifiers": ["public"],
      "type": "boolean",
      "annotations": [{"name": "Override"}],
      "parameters": [{
        "name": "lookupId",
        "type": "Identifier"
      }],
      "body": "{\n    if (!delegate.canViewOptions(lookupId)) {\n        return false;\n    }\n    /* anybody can view options */\n    return true;\n}"
    },
    {
      "name": "canViewOption",
      "modifiers": ["public"],
      "type": "boolean",
      "annotations": [{"name": "Override"}],
      "parameters": [{
        "name": "optionId",
        "type": "Identifier"
      }],
      "body": "{\n    if (!delegate.canViewOption(optionId)) {\n        return false;\n    }\n    /* anybody can view a option */\n    return true;\n}"
    },
    {
      "name": "canUpdateOption",
      "modifiers": ["public"],
      "type": "boolean",
      "annotations": [{"name": "Override"}],
      "parameters": [{
        "name": "optionId",
        "type": "Identifier"
      }],
      "body": "{\n    if (!delegate.canUpdateOption(optionId)) {\n        return false;\n    }\n    /* only a CRM Admin can update a Option */\n    return auth.isUserInRole(SYS_ADMIN);\n}"
    },
    {
      "name": "canEnableOption",
      "modifiers": ["public"],
      "type": "boolean",
      "annotations": [{"name": "Override"}],
      "parameters": [{
        "name": "optionId",
        "type": "Identifier"
      }],
      "body": "{\n    if (!delegate.canEnableOption(optionId)) {\n        return false;\n    }\n    /* only a CRM Admin can enable a Option */\n    return auth.isUserInRole(SYS_ADMIN);\n}"
    },
    {
      "name": "canDisableOption",
      "modifiers": ["public"],
      "type": "boolean",
      "annotations": [{"name": "Override"}],
      "parameters": [{
        "name": "optionId",
        "type": "Identifier"
      }],
      "body": "{\n    if (!delegate.canDisableOption(optionId)) {\n        return false;\n    }\n    /* only a CRM Admin can disable a Option */\n    return auth.isUserInRole(SYS_ADMIN);\n}"
    }
  ]
}