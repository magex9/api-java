package ca.magex.crm.api.decorators;

/**
 * AUTO-GENERATED: This file is auto-generated by ca.magex.json.javadoc.JavadocInterfaceAdapterBuilder
 * 
 * Logging and delegate decorators for the CRM services and policies
 * 
 * This class enables to you to inject nicely formatted slf4j logging onto any class you want.
 * <dl>
 *     <dd>TRACE</dd><dt>Log the method call with parameters including the result and execution time.</dt>
 *     <dd>DEBUG</dd><dt>Log the method call with parameters with the execution time.</dt>
 *     <dd>INFO</dd><dt>Log the method call with parameters.</dt>
 *     <dd>WARNING</dd><dt>No logging</dt>
 *     <dd>ERROR</dd><dt>No logging</dt>
 * </dl>
 * 
 * @author magex
 */
public class CrmPersonPolicySlf4jDecorator implements ca.magex.crm.api.policies.CrmPersonPolicy {
	
	private ca.magex.crm.api.policies.CrmPersonPolicy delegate;
	
	private org.slf4j.Logger logger;
	
	public CrmPersonPolicySlf4jDecorator(ca.magex.crm.api.policies.CrmPersonPolicy delegate, org.slf4j.Logger logger) {
		this.delegate = delegate;
		this.logger = logger;
	}
	
	@Override
	public boolean canCreatePersonForOrganization(ca.magex.crm.api.system.Identifier organizationId) {
		if (logger.isTraceEnabled()) {
			long start = System.nanoTime();
			try {
				logger.trace("Calling canCreatePersonForOrganization(" + organizationId + ")");
				boolean result = delegate.canCreatePersonForOrganization(organizationId);
				logger.trace("Executed canCreatePersonForOrganization(" + organizationId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + "returnType" + ": " + result + ").");
				return result;
			}
			catch (Exception e) {
				logger.trace("Exception on canCreatePersonForOrganization(" + organizationId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isDebugEnabled()) {
			long start = System.nanoTime();
			try {
				logger.debug("Calling canCreatePersonForOrganization(" + organizationId + ")");
				boolean result = delegate.canCreatePersonForOrganization(organizationId);
				logger.debug("Executed canCreatePersonForOrganization(" + organizationId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + ".");
				return result;
			}
			catch (Exception e) {
				logger.debug("Exception on canCreatePersonForOrganization(" + organizationId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isInfoEnabled()) {
			logger.info("Calling canCreatePersonForOrganization(" + organizationId + ")");
			return delegate.canCreatePersonForOrganization(organizationId);
		}
		else {
			return delegate.canCreatePersonForOrganization(organizationId);
		}
	}
	
	@Override
	public boolean canViewPerson(ca.magex.crm.api.system.Identifier personId) {
		if (logger.isTraceEnabled()) {
			long start = System.nanoTime();
			try {
				logger.trace("Calling canViewPerson(" + personId + ")");
				boolean result = delegate.canViewPerson(personId);
				logger.trace("Executed canViewPerson(" + personId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + "returnType" + ": " + result + ").");
				return result;
			}
			catch (Exception e) {
				logger.trace("Exception on canViewPerson(" + personId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isDebugEnabled()) {
			long start = System.nanoTime();
			try {
				logger.debug("Calling canViewPerson(" + personId + ")");
				boolean result = delegate.canViewPerson(personId);
				logger.debug("Executed canViewPerson(" + personId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + ".");
				return result;
			}
			catch (Exception e) {
				logger.debug("Exception on canViewPerson(" + personId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isInfoEnabled()) {
			logger.info("Calling canViewPerson(" + personId + ")");
			return delegate.canViewPerson(personId);
		}
		else {
			return delegate.canViewPerson(personId);
		}
	}
	
	@Override
	public boolean canUpdatePerson(ca.magex.crm.api.system.Identifier personId) {
		if (logger.isTraceEnabled()) {
			long start = System.nanoTime();
			try {
				logger.trace("Calling canUpdatePerson(" + personId + ")");
				boolean result = delegate.canUpdatePerson(personId);
				logger.trace("Executed canUpdatePerson(" + personId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + "returnType" + ": " + result + ").");
				return result;
			}
			catch (Exception e) {
				logger.trace("Exception on canUpdatePerson(" + personId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isDebugEnabled()) {
			long start = System.nanoTime();
			try {
				logger.debug("Calling canUpdatePerson(" + personId + ")");
				boolean result = delegate.canUpdatePerson(personId);
				logger.debug("Executed canUpdatePerson(" + personId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + ".");
				return result;
			}
			catch (Exception e) {
				logger.debug("Exception on canUpdatePerson(" + personId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isInfoEnabled()) {
			logger.info("Calling canUpdatePerson(" + personId + ")");
			return delegate.canUpdatePerson(personId);
		}
		else {
			return delegate.canUpdatePerson(personId);
		}
	}
	
	@Override
	public boolean canEnablePerson(ca.magex.crm.api.system.Identifier personId) {
		if (logger.isTraceEnabled()) {
			long start = System.nanoTime();
			try {
				logger.trace("Calling canEnablePerson(" + personId + ")");
				boolean result = delegate.canEnablePerson(personId);
				logger.trace("Executed canEnablePerson(" + personId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + "returnType" + ": " + result + ").");
				return result;
			}
			catch (Exception e) {
				logger.trace("Exception on canEnablePerson(" + personId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isDebugEnabled()) {
			long start = System.nanoTime();
			try {
				logger.debug("Calling canEnablePerson(" + personId + ")");
				boolean result = delegate.canEnablePerson(personId);
				logger.debug("Executed canEnablePerson(" + personId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + ".");
				return result;
			}
			catch (Exception e) {
				logger.debug("Exception on canEnablePerson(" + personId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isInfoEnabled()) {
			logger.info("Calling canEnablePerson(" + personId + ")");
			return delegate.canEnablePerson(personId);
		}
		else {
			return delegate.canEnablePerson(personId);
		}
	}
	
	@Override
	public boolean canDisablePerson(ca.magex.crm.api.system.Identifier personId) {
		if (logger.isTraceEnabled()) {
			long start = System.nanoTime();
			try {
				logger.trace("Calling canDisablePerson(" + personId + ")");
				boolean result = delegate.canDisablePerson(personId);
				logger.trace("Executed canDisablePerson(" + personId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + "returnType" + ": " + result + ").");
				return result;
			}
			catch (Exception e) {
				logger.trace("Exception on canDisablePerson(" + personId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isDebugEnabled()) {
			long start = System.nanoTime();
			try {
				logger.debug("Calling canDisablePerson(" + personId + ")");
				boolean result = delegate.canDisablePerson(personId);
				logger.debug("Executed canDisablePerson(" + personId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + ".");
				return result;
			}
			catch (Exception e) {
				logger.debug("Exception on canDisablePerson(" + personId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isInfoEnabled()) {
			logger.info("Calling canDisablePerson(" + personId + ")");
			return delegate.canDisablePerson(personId);
		}
		else {
			return delegate.canDisablePerson(personId);
		}
	}
	
}
