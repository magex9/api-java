package ca.magex.crm.api.decorators;

/**
 * AUTO-GENERATED: This file is auto-generated by ca.magex.json.javadoc.JavadocInterfaceAdapterBuilder
 * 
 * Logging and delegate decorators for the CRM services and policies
 * 
 * This class enables to you to inject nicely formatted slf4j logging onto any class you want.
 * <dl>
 *     <dd>TRACE</dd><dt>Log the method call with parameters including the result and execution time.</dt>
 *     <dd>DEBUG</dd><dt>Log the method call with parameters with the execution time.</dt>
 *     <dd>INFO</dd><dt>Log the method call with parameters.</dt>
 *     <dd>WARNING</dd><dt>No logging</dt>
 *     <dd>ERROR</dd><dt>No logging</dt>
 * </dl>
 * 
 * @author magex
 */
public class CrmRolePolicySlf4jDecorator implements ca.magex.crm.api.policies.CrmRolePolicy {
	
	private ca.magex.crm.api.policies.CrmRolePolicy delegate;
	
	private org.slf4j.Logger logger;
	
	public CrmRolePolicySlf4jDecorator(ca.magex.crm.api.policies.CrmRolePolicy delegate, org.slf4j.Logger logger) {
		this.delegate = delegate;
		this.logger = logger;
	}
	
	@Override
	public boolean canCreateRole(ca.magex.crm.api.system.Identifier groupId) {
		if (logger.isTraceEnabled()) {
			long start = System.nanoTime();
			try {
				logger.trace("Calling canCreateRole(" + groupId + ")");
				boolean result = delegate.canCreateRole(groupId);
				logger.trace("Executed canCreateRole(" + groupId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + "returnType" + ": " + result + ").");
				return result;
			}
			catch (Exception e) {
				logger.trace("Exception on canCreateRole(" + groupId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isDebugEnabled()) {
			long start = System.nanoTime();
			try {
				logger.debug("Calling canCreateRole(" + groupId + ")");
				boolean result = delegate.canCreateRole(groupId);
				logger.debug("Executed canCreateRole(" + groupId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + ".");
				return result;
			}
			catch (Exception e) {
				logger.debug("Exception on canCreateRole(" + groupId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isInfoEnabled()) {
			logger.info("Calling canCreateRole(" + groupId + ")");
			return delegate.canCreateRole(groupId);
		}
		else {
			return delegate.canCreateRole(groupId);
		}
	}
	
	@Override
	public boolean canViewRoles() {
		if (logger.isTraceEnabled()) {
			long start = System.nanoTime();
			try {
				logger.trace("Calling canViewRoles()");
				boolean result = delegate.canViewRoles();
				logger.trace("Executed canViewRoles() in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + "returnType" + ": " + result + ").");
				return result;
			}
			catch (Exception e) {
				logger.trace("Exception on canViewRoles() in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isDebugEnabled()) {
			long start = System.nanoTime();
			try {
				logger.debug("Calling canViewRoles()");
				boolean result = delegate.canViewRoles();
				logger.debug("Executed canViewRoles() in " + java.time.Duration.ofNanos(System.nanoTime() - start) + ".");
				return result;
			}
			catch (Exception e) {
				logger.debug("Exception on canViewRoles() in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isInfoEnabled()) {
			logger.info("Calling canViewRoles()");
			return delegate.canViewRoles();
		}
		else {
			return delegate.canViewRoles();
		}
	}
	
	@Override
	public boolean canViewRole(String code) {
		if (logger.isTraceEnabled()) {
			long start = System.nanoTime();
			try {
				logger.trace("Calling canViewRole(" + code + ")");
				boolean result = delegate.canViewRole(code);
				logger.trace("Executed canViewRole(" + code + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + "returnType" + ": " + result + ").");
				return result;
			}
			catch (Exception e) {
				logger.trace("Exception on canViewRole(" + code + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isDebugEnabled()) {
			long start = System.nanoTime();
			try {
				logger.debug("Calling canViewRole(" + code + ")");
				boolean result = delegate.canViewRole(code);
				logger.debug("Executed canViewRole(" + code + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + ".");
				return result;
			}
			catch (Exception e) {
				logger.debug("Exception on canViewRole(" + code + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isInfoEnabled()) {
			logger.info("Calling canViewRole(" + code + ")");
			return delegate.canViewRole(code);
		}
		else {
			return delegate.canViewRole(code);
		}
	}
	
	@Override
	public boolean canViewRole(ca.magex.crm.api.system.Identifier roleId) {
		if (logger.isTraceEnabled()) {
			long start = System.nanoTime();
			try {
				logger.trace("Calling canViewRole(" + roleId + ")");
				boolean result = delegate.canViewRole(roleId);
				logger.trace("Executed canViewRole(" + roleId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + "returnType" + ": " + result + ").");
				return result;
			}
			catch (Exception e) {
				logger.trace("Exception on canViewRole(" + roleId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isDebugEnabled()) {
			long start = System.nanoTime();
			try {
				logger.debug("Calling canViewRole(" + roleId + ")");
				boolean result = delegate.canViewRole(roleId);
				logger.debug("Executed canViewRole(" + roleId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + ".");
				return result;
			}
			catch (Exception e) {
				logger.debug("Exception on canViewRole(" + roleId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isInfoEnabled()) {
			logger.info("Calling canViewRole(" + roleId + ")");
			return delegate.canViewRole(roleId);
		}
		else {
			return delegate.canViewRole(roleId);
		}
	}
	
	@Override
	public boolean canUpdateRole(ca.magex.crm.api.system.Identifier roleId) {
		if (logger.isTraceEnabled()) {
			long start = System.nanoTime();
			try {
				logger.trace("Calling canUpdateRole(" + roleId + ")");
				boolean result = delegate.canUpdateRole(roleId);
				logger.trace("Executed canUpdateRole(" + roleId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + "returnType" + ": " + result + ").");
				return result;
			}
			catch (Exception e) {
				logger.trace("Exception on canUpdateRole(" + roleId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isDebugEnabled()) {
			long start = System.nanoTime();
			try {
				logger.debug("Calling canUpdateRole(" + roleId + ")");
				boolean result = delegate.canUpdateRole(roleId);
				logger.debug("Executed canUpdateRole(" + roleId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + ".");
				return result;
			}
			catch (Exception e) {
				logger.debug("Exception on canUpdateRole(" + roleId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isInfoEnabled()) {
			logger.info("Calling canUpdateRole(" + roleId + ")");
			return delegate.canUpdateRole(roleId);
		}
		else {
			return delegate.canUpdateRole(roleId);
		}
	}
	
	@Override
	public boolean canEnableRole(ca.magex.crm.api.system.Identifier roleId) {
		if (logger.isTraceEnabled()) {
			long start = System.nanoTime();
			try {
				logger.trace("Calling canEnableRole(" + roleId + ")");
				boolean result = delegate.canEnableRole(roleId);
				logger.trace("Executed canEnableRole(" + roleId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + "returnType" + ": " + result + ").");
				return result;
			}
			catch (Exception e) {
				logger.trace("Exception on canEnableRole(" + roleId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isDebugEnabled()) {
			long start = System.nanoTime();
			try {
				logger.debug("Calling canEnableRole(" + roleId + ")");
				boolean result = delegate.canEnableRole(roleId);
				logger.debug("Executed canEnableRole(" + roleId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + ".");
				return result;
			}
			catch (Exception e) {
				logger.debug("Exception on canEnableRole(" + roleId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isInfoEnabled()) {
			logger.info("Calling canEnableRole(" + roleId + ")");
			return delegate.canEnableRole(roleId);
		}
		else {
			return delegate.canEnableRole(roleId);
		}
	}
	
	@Override
	public boolean canDisableRole(ca.magex.crm.api.system.Identifier roleId) {
		if (logger.isTraceEnabled()) {
			long start = System.nanoTime();
			try {
				logger.trace("Calling canDisableRole(" + roleId + ")");
				boolean result = delegate.canDisableRole(roleId);
				logger.trace("Executed canDisableRole(" + roleId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + "returnType" + ": " + result + ").");
				return result;
			}
			catch (Exception e) {
				logger.trace("Exception on canDisableRole(" + roleId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isDebugEnabled()) {
			long start = System.nanoTime();
			try {
				logger.debug("Calling canDisableRole(" + roleId + ")");
				boolean result = delegate.canDisableRole(roleId);
				logger.debug("Executed canDisableRole(" + roleId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + ".");
				return result;
			}
			catch (Exception e) {
				logger.debug("Exception on canDisableRole(" + roleId + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isInfoEnabled()) {
			logger.info("Calling canDisableRole(" + roleId + ")");
			return delegate.canDisableRole(roleId);
		}
		else {
			return delegate.canDisableRole(roleId);
		}
	}
	
}
