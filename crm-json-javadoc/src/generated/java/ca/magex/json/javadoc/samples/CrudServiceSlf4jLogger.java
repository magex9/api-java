package ca.magex.json.javadoc.samples;

/**
 * AUTO-GENERATED: This file is auto-generated by ca.magex.json.javadoc.JavadocInterfaceAdapterBuilder
 * 
 * JUnit Crud Service Logger Test
 * 
 * This class enables to you to inject nicely formatted slf4j logging onto any class you want.
 * <dl>
 *     <dd>TRACE</dd><dt>Log the method call with parameters including the result and execution time.</dt>
 *     <dd>DEBUG</dd><dt>Log the method call with parameters with the execution time.</dt>
 *     <dd>INFO</dd><dt>Log the method call with parameters.</dt>
 *     <dd>WARNING</dd><dt>No logging</dt>
 *     <dd>ERROR</dd><dt>No logging</dt>
 * </dl>
 * 
 * @author magex
 */
public class CrudServiceSlf4jLogger<K, T> implements ca.magex.json.javadoc.samples.CrudService<K, T> {
	
	private ca.magex.json.javadoc.samples.CrudService<K, T> delegate;
	
	private org.slf4j.Logger logger;
	
	public CrudServiceSlf4jLogger(ca.magex.json.javadoc.samples.CrudService<K, T> delegate, org.slf4j.Logger logger) {
		this.delegate = delegate;
		this.logger = logger;
	}
	
	@Override
	public T load(K id) throws java.util.NoSuchElementException {
		if (logger.isTraceEnabled()) {
			long start = System.nanoTime();
			try {
				logger.trace("Calling load(" + id + ")");
				T result = delegate.load(id);
				logger.trace("Executed load(" + id + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + result.getClass() + ": " + result + ").");
				return result;
			}
			catch (Exception e) {
				logger.trace("Exception on load(" + id + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isDebugEnabled()) {
			long start = System.nanoTime();
			try {
				logger.debug("Calling load(" + id + ")");
				T result = delegate.load(id);
				logger.debug("Executed load(" + id + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + ".");
				return result;
			}
			catch (Exception e) {
				logger.debug("Exception on load(" + id + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isInfoEnabled()) {
			logger.info("Calling load(" + id + ")");
			return delegate.load(id);
		}
		else {
			return delegate.load(id);
		}
	}
	
	@Override
	public java.util.List<T> findAll() {
		if (logger.isTraceEnabled()) {
			long start = System.nanoTime();
			try {
				logger.trace("Calling findAll()");
				java.util.List<T> result = delegate.findAll();
				logger.trace("Executed findAll() in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + "returnType" + ": " + result + ").");
				return result;
			}
			catch (Exception e) {
				logger.trace("Exception on findAll() in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isDebugEnabled()) {
			long start = System.nanoTime();
			try {
				logger.debug("Calling findAll()");
				java.util.List<T> result = delegate.findAll();
				logger.debug("Executed findAll() in " + java.time.Duration.ofNanos(System.nanoTime() - start) + ".");
				return result;
			}
			catch (Exception e) {
				logger.debug("Exception on findAll() in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isInfoEnabled()) {
			logger.info("Calling findAll()");
			return delegate.findAll();
		}
		else {
			return delegate.findAll();
		}
	}
	
	@Override
	public void update(T entity) {
		if (logger.isTraceEnabled()) {
			long start = System.nanoTime();
			try {
				logger.trace("Calling update(" + entity + ")");
				delegate.update(entity);
				logger.trace("Executed update(" + entity + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + ".");
				return;
			}
			catch (Exception e) {
				logger.trace("Exception on update(" + entity + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isDebugEnabled()) {
			long start = System.nanoTime();
			try {
				logger.debug("Calling update(" + entity + ")");
				delegate.update(entity);
				logger.debug("Executed update(" + entity + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + ".");
				return;
			}
			catch (Exception e) {
				logger.debug("Exception on update(" + entity + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isInfoEnabled()) {
			logger.info("Calling update(" + entity + ")");
			delegate.update(entity);
		}
		else {
			delegate.update(entity);
		}
	}
	
	@Override
	public boolean contains(K id) {
		if (logger.isTraceEnabled()) {
			long start = System.nanoTime();
			try {
				logger.trace("Calling contains(" + id + ")");
				boolean result = delegate.contains(id);
				logger.trace("Executed contains(" + id + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + "returnType" + ": " + result + ").");
				return result;
			}
			catch (Exception e) {
				logger.trace("Exception on contains(" + id + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isDebugEnabled()) {
			long start = System.nanoTime();
			try {
				logger.debug("Calling contains(" + id + ")");
				boolean result = delegate.contains(id);
				logger.debug("Executed contains(" + id + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + ".");
				return result;
			}
			catch (Exception e) {
				logger.debug("Exception on contains(" + id + ") in " + java.time.Duration.ofNanos(System.nanoTime() - start) + " (" + e.getClass() + ": " + e.getMessage() + ").");
				throw e;
			}
		}
		else if (logger.isInfoEnabled()) {
			logger.info("Calling contains(" + id + ")");
			return delegate.contains(id);
		}
		else {
			return delegate.contains(id);
		}
	}
	
}
