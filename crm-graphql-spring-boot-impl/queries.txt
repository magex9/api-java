# Find all Organizations

{
	findOrganizations(
					paging: {
						pageNumber: 2,
						pageSize: 3,
						sortField: "displayName",
						sortOrder: DESC
					}) { 
		totalPages
		totalElements
		numberOfElements
		content {
			organizationId
			status
			displayName
			mainLocation {
				displayName
				address {
					street
					city
					province
					country
					postalCode
				}
			}
		}
	}
}

# Find Organization By ID
}
	findOrganization(organizationId: "srVujtEUmb") {
		organizationId
		status
		mainLocation {    
			displayName
		}
	}
}

# Create Organization
mutation {
	createOrganization(organizationName: "Jonny Org") {
		organizationId
		status
	}
}

# Create Location
mutation {
	createLocation(
				organizationId: "???", 
				locationName: "???", 
				locationReference: "???",
				locationAddress: {
					street: "???",
					city: "???",
					province: "???",
					countryCode: "???",
					postalCode: "???"
				}) {
		organizationId
		status
	}
}